#include<stdio.h>
void main(){
	int i,n,arr[i];
	printf("enter a number in element: ");
	scanf("%d",&n);
	int number[n];
	printf("enter the element of the array:\n ");
	for(i=0;i<n;i++){
		printf("enter element into arr[%d]:",i);
		scanf("%d",&number[i]);
    }
    printf("arr[%d]",number[i]);
}
