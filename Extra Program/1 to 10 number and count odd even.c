#include<stdio.h>
void main()
{
	int n,i=1,evencount=0,oddcount=0;
	while(i<=10)
	{
		printf("enter a number :");
		scanf("%d",&n);
		if(i%2)
		oddcount++=1;
		else
		even count++=1
		i++;
	}
	printf("total evennumber is %d",even count);
	printf("total odd number is %d",odd count);
}
